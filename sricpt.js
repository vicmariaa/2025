const forma = "quadrado"
const altura = 5 
const comprimento = 7
let area;

if (forma === "quadrado"){
    area = (altura * comprimento)
}
else{
    area = (altura * comprimento)
} 

console.log (area)