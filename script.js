const forma = "quadrado"
const altura = 5 
const comprimento = 7
let arena;

if (forma === "quadrado"){
    area = (altura * comprimento)
}
else{
    area = (altura * comprimento)
} 

console.log (area)